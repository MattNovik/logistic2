extends ../base/layout

block variables
  - const title = "Logistic" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = ""
  - const currentPage = "main" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main
    .container
      +slider(sliderData)
      +commonInfo(infoData)
      .world-map-wrapper
        +modulesTypes(modulesData)
        +feedback(feedbackData)

block modals
